from distutils.core import setup
setup(
  name = 'seash',
  packages = [''], 
  version = '0.1',
  description = "Seattle's seash package",
  author = 'Seattle',
  url = 'https://github.com/choksi81/seash', # use the URL to the github repo
  classifiers = [],
)